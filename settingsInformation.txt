Settings Information

checkDOB - if true, then creating certain accounts will require the user to be over a set age. 
         - if false, then there is no age restriction on any account
         
balanceAlerts - if true, then user will be be notified when their balance reaches below $50 
              - if false, then user will not recieve any notifications when their balance reaches below $50
              
